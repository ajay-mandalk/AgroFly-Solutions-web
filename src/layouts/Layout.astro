---

import BackToTop from "../components/layout/BackToTop.astro";
import MadeWith from "../components/layout/MadeWith.astro";


export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="en">
  <head>
   <!-- Basic Meta Tags -->
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<meta name="description" content="AgroFly offers cutting-edge drone spraying and crop disease detection services for smarter, faster, and sustainable farming." />
<meta name="keywords" content="AgroFly, agriculture drones, drone spraying, smart farming, crop health, agri-tech, drone services, sustainable farming" />
<meta name="author" content="AgroFly Team" />
<meta name="generator" content={Astro.generator} />

<!-- Title -->
<title>AgroFly Solutions – {title || 'Smart Agriculture with Drones'}</title>

<!-- Canonical URL -->
<link rel="canonical" href="https://www.agrofly.in/" />

<!-- Favicon -->
<link rel="icon" type="image/svg+xml" href="/favicon.svg" />

<!-- Open Graph Meta (Facebook, LinkedIn) -->
<meta property="og:title" content="AgroFly Solutions – Smart Drone Farming" />
<meta property="og:description" content="Empowering Indian farmers with drone spraying and disease detection using AI-powered technology." />
<meta property="og:image" content="https://www.agrofly.in/images/og-banner.jpg" />
<meta property="og:url" content="https://www.agrofly.in/" />
<meta property="og:type" content="website" />

<!-- Twitter Meta -->
<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:title" content="AgroFly Solutions – Smarter Farming with Drones" />
<meta name="twitter:description" content="Book drone spraying or detect crop diseases with AgroFly’s precision farming solutions." />
<meta name="twitter:image" content="https://www.agrofly.in/images/og-banner.jpg" />

<!-- Optional: Structured Data (for SEO Rich Results) -->
<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Organization",
  "name": "AgroFly Solutions",
  "url": "https://www.agrofly.in/",
  "logo": "https://www.agrofly.in/logo.png",
  "sameAs": [
    "https://twitter.com/agrofly",
    "https://www.linkedin.com/company/agrofly"
  ],
  "description": "AgroFly is a smart agri-tech platform offering drone spraying, crop disease detection, and data-driven farming solutions across India."
}
</script>

    <link
      href="https://fonts.googleapis.com/css2?family=Raleway:wght@300;400;500;600;700&display=swap"
      rel="stylesheet"
    />
  </head>
  <body class="overflow-hidden overflow-y-auto bg-body">

    <slot />
  
    <BackToTop />
    <MadeWith />
  </body>
  <script>
    const switchTheme = document.querySelector(
      "[data-switch-theme]"
    ) as HTMLButtonElement;

    if (
      localStorage.getItem("appTheme") === "dark" ||
      (!("appTheme" in localStorage) &&
        window.matchMedia("(prefers-color-scheme: dark)").matches)
    ) {
      document.documentElement.classList.add("dark");
    } else {
      document.documentElement.classList.remove("dark");
    }
    if (switchTheme) {
      switchTheme.addEventListener("click", (e) => {
        e.preventDefault();
        const doc = document.documentElement;
        if (doc) {
          if (localStorage.getItem("appTheme")) {
            if (localStorage.getItem("appTheme") === "light") {
              doc.classList.add("dark");
              localStorage.setItem("appTheme", "dark");
            } else {
              document.documentElement.classList.remove("dark");
              localStorage.setItem("appTheme", "light");
            }
          } else {
            if (doc.classList.contains("dark")) {
              doc.classList.remove("dark");
              localStorage.setItem("appTheme", "light");
            } else {
              doc.classList.add("dark");
              localStorage.setItem("appTheme", "dark");
            }
          }
        }
      });
    }
  </script>
  <script>
    const toggleMenu = document.querySelector(
      "[data-toggle-nav]"
    ) as HTMLButtonElement;
    const navbar = document.querySelector("[data-navbar]") as HTMLDivElement;
    const overlayNav = document.querySelector(
      "[data-nav-overlay]"
    ) as HTMLDivElement;
    if (toggleMenu && navbar && overlayNav) {
      toggleMenu.addEventListener("click", e => {
        e.preventDefault();
        if (toggleMenu.getAttribute("data-open-nav") === "false") {
          toggleMenu.setAttribute("data-open-nav", "true");
          overlayNav.setAttribute("data-is-visible", "true");
          document.body.classList.add("!overflow-y-hidden");
		  navbar.style.height = `${navbar.scrollHeight}px`

        }else{
			toggleMenu.setAttribute("data-open-nav", "false");
        	overlayNav.setAttribute("data-is-visible", "false");
        	document.body.classList.remove("!overflow-y-hidden");
		  	navbar.style.height = "0px"
		}
		
        
      });

      navbar.addEventListener("click", () => {
        toggleMenu.setAttribute("data-open-nav", "false");
        overlayNav.setAttribute("data-is-visible", "false");
        document.body.classList.remove("!overflow-y-hidden");
		  	navbar.style.height = "0px"
      });

      overlayNav.addEventListener("click", () => {
        toggleMenu.setAttribute("data-open-nav", "false");
        	overlayNav.setAttribute("data-is-visible", "false");
        	document.body.classList.remove("!overflow-y-hidden");
		  	navbar.style.height = "0px"
      });
    }
    import "../scripts/stickymanu.js";
  </script>
</html>
<style is:global>
  /**
   * Defines CSS custom properties (variables) for colors used throughout the site.
   * Includes background, border, box, and heading colors.
   */
  :root {
    --color-bg: 255 255 255;
    --color-border: 255 255 255;
    --color-box: 255 255 255;
    --box-border: 229 231 235;
    --box-sd: 226 232 240 / 0.5;
    --heading-1: 23 37 84;
    --heading-2: 31 41 55;
    --heading-3: 55 65 81;
  }

    /**
   * Defines CSS custom property variables for dark mode colors.
   * Includes background, border, box, and heading colors.
   */
  .dark {
    --color-bg: 3 7 18;
    --color-box: 17 24 39;
    --box-border: 243 244 246/0.1;
    --box-sd: transparent;

    --heading-1: 255 255 255;
    --heading-2: 243 244 246;
    --heading-3: 209 213 219;
  }

  html{
    scroll-behavior: smooth;
  }
  body {
    font-family: "Raleway", sans-serif;
  }

  [data-toggle-nav][data-open-nav="true"] #line1 {
    transform: translateY(0.375rem) rotate(40deg);
  }
  [data-toggle-nav][data-open-nav="true"] #line2 {
    transform: scaleX(0);
    opacity: 0;
  }
  [data-toggle-nav][data-open-nav="true"] #line3 {
    transform: translateY(-0.375rem) rotate(-40deg);
  }

  
  [data-nav-overlay][data-is-visible="true"] {
    visibility: visible;
    display: flex;
  }
</style>
